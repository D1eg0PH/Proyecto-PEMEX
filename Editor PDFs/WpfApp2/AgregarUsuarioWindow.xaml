<Window x:Class="WpfApp2.AgregarUsuarioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agregar Usuario y Firma" Height="500" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="txtNombre" Grid.Row="1" Margin="0,0,0,10"/>

        <TextBlock Text="Rol:" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="txtRol" Grid.Row="3" Margin="0,0,0,10"/>

        <TextBlock Text="Contraseña:" Grid.Row="4" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtContrasena" Grid.Row="5" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Seleccionar Firma (PNG)" Click="BtnSeleccionarFirma_Click" Width="180" Margin="0,0,10,0"/>
            <TextBlock x:Name="lblFirmaPath" Text="Ningún archivo seleccionado" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Guardar" Click="BtnGuardar_Click" Width="80" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Cancelar" Click="BtnCancelar_Click" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>